{% extends 'base.html.twig' %}

{% block title %}Catégorie non trouvée{% endblock %}

{% block body %}
    <div class="container py-4">
        <div class="row">
            <!-- Sidebar avec les catégories -->
            <div class="col-md-3">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="card-title mb-0">Catégories</h5>
                    </div>
                    <div class="list-group list-group-flush">
                        {% for cat in categories %}
                            <a href="{{ path('app_article_by_category', {'name': cat.name}) }}" 
                               class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                                {{ cat.name }}
                                <span class="badge bg-primary rounded-pill">{{ cat.articles|length }}</span>
                            </a>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Message d'erreur -->
            <div class="col-md-9">
                <div class="alert alert-warning">
                    <h4 class="alert-heading">Catégorie non trouvée</h4>
                    <p>La catégorie <strong>"{{ category_name }}"</strong> n'existe pas ou n'est pas accessible.</p>
                    <hr>
                    <p class="mb-0">Vous pouvez choisir une catégorie dans la liste à gauche ou <a href="{{ path('app_article_index') }}" class="alert-link">retourner à la liste des articles</a>.</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
